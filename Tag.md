# Make your changes
git add .
git commit -m "Fix missing feature"

# Create a new version tag
git tag -a v1.0.1 -m "Bug fixes"

# Push the commit and the new tag
git push
git push origin v1.0.1


Sujet : 
Notion d'epargne:

Le client a une interface pour definir l'epargne (ex 50 , enregistre base)
Izay transfert mankany any @ dia 50 % mankany epargne et 50% solde principale

